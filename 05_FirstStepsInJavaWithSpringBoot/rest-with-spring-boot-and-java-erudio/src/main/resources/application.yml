
spring:
  datasource:
    jdbcUrl: jdbc:postgresql://localhost:5432/rest_spring_java_erudio
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/rest_spring_java_erudio?useTimezone=true&serveTimezone=UTC
    username: postgres
    password: logique
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: false


